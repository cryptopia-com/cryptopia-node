version: '3.8'
services:
  cryptopia-node:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: "host"
    environment:
      - PUBLIC_IP=0.0.0.0
      - APPLICATION_INSIGHTS_CONNECTION_STRING
    stdin_open: true
    tty: true
    env_file:
      - .env